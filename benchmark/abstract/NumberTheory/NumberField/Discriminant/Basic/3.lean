import Mathlib

open NumberField

/-- If a number field `K` has discriminant `±1`, then its ring of integers is monogenic
generated by an element of discriminant `±1`. -/
lemma exists_integral_basis_singleton_of_abs_discr_le_one
    (K : Type*) [Field K] [NumberField K]
    (h : |NumberField.discr K| ≤ 1) :
    ∃ α : 𝓞 K, Algebra.adjoin ℤ ({α} : Set (𝓞 K)) = ⊤ := by
  sorry