import Mathlib

open CategoryTheory

/-- A connected component of an object in a Galois category is the image of some Galois object. -/
lemma connected_component_is_image_of_galois
    {C : Type u} [Category C] [PreGaloisCategory C] :
    True := by
  sorry