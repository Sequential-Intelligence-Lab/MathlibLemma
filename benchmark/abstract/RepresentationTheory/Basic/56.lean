import Mathlib


/-
The lemmas below are intentionally left with `by sorry`.
They are meant as potential additions to mathlib and are not proved here.
-/
-- 57. Fixed submodule of leftRegular is span of sum of group elements
lemma Representation.leftRegular_fixedSubmodule
    {k G : Type*} [Field k] [Group G] [Fintype G] :
    True := by
  -- placeholder for the 1-dimensional space of invariant vectors
  sorry
